'use strict';

module.exports = function (x) {
  return x.replace(/^[\r\n]+/, '').replace(/[\r\n]+$/, '');
};
module.exports.start = function (x) {
  return x.replace(/^[\r\n]+/, '');
};
module.exports.end = function (x) {
  return x.replace(/[\r\n]+$/, '');
};